machine:
  services:
    - docker
    
dependencies:
  pre:
    - docker pull skippa/docker-pebble
    - mkdir .pebble-sdk 
    - chmod g=rwx .pebble-sdk
    - docker run --rm -it -v .pebble-sdk:/home/pebble/.pebble-sdk skippa/docker-pebble sdk install latest
    
test:
  post:
    - echo "hello"
