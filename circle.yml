machine:
  services:
    - docker
    
dependencies:
  pre:
    - docker pull andredumas/pebble-dev
    
test:
  post:
    - cd app
    - docker run --rm -it -v $PWD:/pebble --entrypoint /bin/bash skippa/docker-pebble "/usr/bin/python /tool/pebble.py sdk install latest"
