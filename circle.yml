machine:
  services:
    - docker
    
dependencies:
  pre:
    - docker pull skippa/docker-pebble-sdk
    
test:
  post:
    - docker build -t pebble-today-is . 
    - docker run -ti -v $CIRCLE_ARTIFACTS:/out pebble-today-is
