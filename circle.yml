machine:
  services:
    - docker
    
dependencies:
  pre:
    - docker pull andredumas/pebble-dev
    
test:
  post:
    - cd app
    - docker run --rm -it -v $PWD:/pebble bboehmke/pebble-dev /bin/sh -c 'yes | pebble sdk install 4.1.4 && pebble build'
