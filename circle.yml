machine:
  services:
    - docker
    
dependencies:
  cache_directories:
    - ~/.pebble-sdk
  pre:
    - docker pull skippa/docker-pebble-sdk
    
test:
  post:
    - cd app
    - docker run --rm -it -v $PWD:/pebble skippa/docker-pebble-sdk pebble build
