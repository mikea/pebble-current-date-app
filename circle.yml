machine:
  services:
    - docker
    
dependencies:
  cache_directories:
    - ~/.pebble-sdk
  pre:
    - docker pull skippa/docker-pebble
    
test:
  post:
    - cd app
    - docker run --rm -it -v $PWD:/pebble skippa/docker-pebble /bin/sh -c "python /tool/pebble.py build"
